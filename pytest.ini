[pytest]
testpaths = tests
# greenfield_prototype/ holds the earlier clean-slate design, kept for
# reference only. Its tests target a different protocol and would collide with
# these on conftest module names.
norecursedirs = greenfield_prototype .venv .git
